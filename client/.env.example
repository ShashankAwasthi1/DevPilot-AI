NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Public production site origin (no trailing slash). Used for metadata's
# metadataBase and absolute Open Graph/canonical URLs - if unset in
# production, those URLs silently fall back to http://localhost:3000.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
